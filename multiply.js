// multiply.js

//Well multiply was the only thing left and we had to change it as well
//because gthis time we are going to check on the cherry-pick concept.
function multiply(a, b) {
    // TODO return
    return a*b
};